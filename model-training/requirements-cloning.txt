# Shared voice-clone tooling
librosa==0.10.2.post1
soundfile==0.12.1
webrtcvad==2.0.10
pydub==0.25.1
openai-whisper>=20231117
speechbrain>=1.0
torchaudio==2.2.2

# Phase 0 + XTTS fallback
TTS==0.22.0
trainer==0.0.36

# Objective evaluation and stress testing
jiwer==3.0.5
pedalboard==0.9.16
pyloudnorm>=0.1.1
onnxruntime==1.17.3

# OpenVoice v2 + MeloTTS Korean zero-shot track.
# Pins verified via GitHub API on 2026-04-22:
#   - OpenVoice has no tagged releases; pin to commit 74a1d147b17a
#     (2025-04-19, "FIX:Demo supports Apple Silicon..."), the latest main.
#   - MeloTTS latest release tag is v0.1.2 (2024-03-01).
# Uncomment the two git+https lines after confirming checkpoints are downloaded
# to ~/tools/{OpenVoice,MeloTTS} per docs/WINDOWS_GPT_SOVITS_SETUP.md.
# git+https://github.com/myshell-ai/OpenVoice.git@74a1d147b17a#egg=openvoice
# git+https://github.com/myshell-ai/MeloTTS.git@v0.1.2#egg=melotts

# Do not upgrade torch from this file. If GPT-SoVITS v4 requires CU126/CU128,
# create a separate GPT-SoVITS venv aligned to the upstream release notes.

# CosyVoice2 / GPT-SoVITS / Phase C engines should use separate pinned venvs.
